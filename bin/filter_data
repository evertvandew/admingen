#!/usr/bin/env python3

import sys
import typing
import json
import argparse
from admingen.data import CsvReader



if __name__ == '__main__':
    parse = argparse.ArgumentParser()
    parse.add_argument('script')
    args = parse.parse_args()
    script = open(args.script).read()
    run(sys.stdin, script, sys.stdout)
